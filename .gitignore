node_modules/
artifacts/
cache/
coverage/
.env

# Local encrypted Genesis Verifier A keystore (never commit)
genesis-verifier-a.keystore.json

# Local encrypted Genesis settlement relayer keystore (never commit)
genesis-relayer.keystore.json

# Locally generated BscScan manual verification artifacts
verification-bundle/
